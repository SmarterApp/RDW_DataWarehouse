'''
Created on Mar 13, 2014

@author: tosako
'''


def register_slave(node_id, url, exchange_name='edmigrate', queue_name='edmigrate_conductor', routing_key='edmigrate.conductor'):
    pass

def acknowledgement_pgpool_disconnected(node_id, url, exchange_name='edmigrate', queue_name='edmigrate_conductor', routing_key='edmigrate.conductor'):
    pass

def acknowledgement_pgpool_connected(node_id, url, exchange_name='edmigrate', queue_name='edmigrate_conductor', routing_key='edmigrate.conductor'):
    pass

def acknowledgement_master_disconnected(node_id, url, exchange_name='edmigrate', queue_name='edmigrate_conductor', routing_key='edmigrate.conductor'):
    pass

def acknowledgement_master_connected(node_id, url, exchange_name='edmigrate', queue_name='edmigrate_conductor', routing_key='edmigrate.conductor'):
    pass
