#!/bin/bash
#
# chkconfig: 345 90 10

# Source function library.
. /etc/rc.d/init.d/functions
PIDFILE="/var/run/edudl2/edudl2-file-trigger.pid"
EDUDL2_TRIGGER=". /opt/virtualenv/udl2/bin/activate; python -m edudl2.udl2.udl_trigger -d -p $PIDFILE"

start() {
	echo -n "Starting edsftp sync: "
	daemon --user udl2 $EDUDL2_TRIGGER
	RETVAL=$?
}

stop() {
	echo -n "Stopping edudl2-trigger: "
	killproc -p $PIDFILE
	RETVAL=$?
}
case $1 in
	start)
		start
	;;
	stop)
		stop
	;;
	*)

	echo $"Usage: edudl2-trigger {start|stop}"
	exit 2
esac
exit $RETVAL
